#!/bin/bash

cd /tmp/rom # Depends on where source got synced
# lets remove unnecessary things, so that no storage issue can occur

# Normal build steps
. build/envsetup.sh
lunch ancient_ysl-user
export CCACHE_DIR=/tmp/ccache
export CCACHE_EXEC=$(which ccache)
export USE_CCACHE=1
ccache -M 50G
ccache -o compression=true
ccache -z

# stuffs
up(){
	curl --upload-file $1 https://transfer.sh/
}

make_metalava(){
	tg_post_msg "<b>Ancient Build Triggered for Redmi S2</b>"
	make api-stubs-docs
	make hiddenapi-lists-docs
	make system-api-stubs-docs
	make test-api-stubs-docs
}

export BOT_MSG_URL="https://api.telegram.org/bot$BOT/sendMessage"
export BOT_BUILD_URL="https://api.telegram.org/bot$BOT/sendDocument"

tg_post_msg() {
        curl -s -X POST "$BOT_MSG_URL" -d chat_id="$CHAT" \
        -d "parse_mode=html" \
        -d text="$1"
}


make_rom(){
		make bacon -j16
		if [ -f out/target/product/ysl/*zip ]; then
			zip=$(up out/target/product/ysl/*zip)
			tg_post_msg "<b>Build Completed</b>%0A%0A<b>Link : </b> <code>"$zip"</code>"
			echo " "
			echo "$zip"
		else
		tg_post_msg "<b>Build Failed, I'm gonna cri naw</b>"
		fi
}

make_metalava
make_rom & sleep 4140 && kill "$!" #this is a 1:20 hours timeout
